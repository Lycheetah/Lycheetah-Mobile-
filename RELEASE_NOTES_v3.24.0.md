## Sol v3.24.0 — Free Tier

Sol now works out of the box. No API key required.

New users get 15 free messages per day via the Sol proxy — DeepSeek-backed, full system prompt, full persona and mode system. Every constitutional property preserved. The experience is complete, not a demo.

When you're ready for unlimited use, add your own API key in Settings. Anthropic, Gemini, OpenAI, Mistral, and OpenRouter all supported. Your keys, your data, your conversations.

### What changed

- Free tier: 15 messages/day with no setup required
- Free tier uses the same system prompt, persona, and mode routing as BYOK
- Banner shows remaining messages and invites key upgrade — never guilt-trips
- Limit resets daily at midnight local time
- Adds your own key in Settings at any point to remove the cap

### The stack unchanged

4 personas × Seeker/Adept modes × 5 AI providers. Tool calling (Wikipedia, DuckDuckGo, calculator, insight vault, web, datetime). Mystery School (17 domains, 192 subjects). Sanctum journal, field tracker, and vault. Streak notifications. Export chat to clipboard.

The sanctuary is open.

// Dev stub — manipulateAsync passes URI through with no compression
module.exports = {
  manipulateAsync: async (uri) => ({ uri, base64: null }),
  SaveFormat: { JPEG: 'jpeg', PNG: 'png', WEBP: 'webp' },
};
